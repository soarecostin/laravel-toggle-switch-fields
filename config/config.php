<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Default Field Name(s)
    |--------------------------------------------------------------------------
    |
    | This value is the default name that you should use for a switchable field
    | This value will be used if you don't specify a different switchable field
    | name by overriding the switchableFields method in the controller
    |
    */

    'default_field' => 'published',

    /*
    |--------------------------------------------------------------------------
    | Use Timestamps
    |--------------------------------------------------------------------------
    |
    | Make this value true if you want to hold the timestamp when toggled
    | using fields like published_at instead of boolean fields
    |
    */

    'use_timestamps' => false,
];
